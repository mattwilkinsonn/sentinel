services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: sentinel
      POSTGRES_USER: sentinel
      POSTGRES_PASSWORD: sentinel
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U sentinel"]
      interval: 5s
      timeout: 3s
      retries: 5

  backend:
    build:
      context: ./sentinel-backend
    ports:
      - "3001:3001"
    env_file: .env
    environment:
      DATABASE_URL: postgresql://sentinel:sentinel@postgres/sentinel
      SENTINEL_API_PORT: "3001"
      RUST_LOG: sentinel_backend=debug
    depends_on:
      postgres:
        condition: service_healthy
    develop:
      watch:
        - action: rebuild
          path: ./sentinel-backend/src
        - action: rebuild
          path: ./sentinel-backend/Cargo.toml

volumes:
  pgdata:
