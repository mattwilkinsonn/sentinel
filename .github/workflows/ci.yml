name: CI

"on":
  push:
    branches: ["**"]

env:
  CARGO_TERM_COLOR: always

jobs:
  contracts:
    name: Move Contracts
    runs-on: ubuntu-24.04-arm
    steps:
      - uses: actions/checkout@v6

      - name: Install Sui CLI
        run: |
          SUI_VERSION=$(curl -sS "https://api.github.com/repos/MystenLabs/sui/releases/latest" | jq -r '.tag_name')
          curl -sSfL "https://github.com/MystenLabs/sui/releases/download/${SUI_VERSION}/sui-${SUI_VERSION}-ubuntu-aarch64.tgz" -o sui.tgz
          tar -xzf sui.tgz
          sudo mv sui /usr/local/bin/
          rm sui.tgz
          sui --version

      - name: Build sentinel contracts
        run: cd move-contracts/sentinel && sui move build

      - name: Test sentinel contracts
        run: cd move-contracts/sentinel && sui move test

  backend:
    name: Rust Backend
    runs-on: ubuntu-24.04-arm
    permissions:
      packages: write
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_DB: sentinel
          POSTGRES_USER: sentinel
          POSTGRES_PASSWORD: sentinel
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U sentinel"
          --health-interval 5s
          --health-timeout 3s
          --health-retries 5
    env:
      DATABASE_URL: postgresql://sentinel:sentinel@localhost/sentinel
    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: sentinel-backend

      - name: Install protoc
        run: sudo apt-get update && sudo apt-get install -y protobuf-compiler

      - name: Clone Sui proto definitions
        run: |
          cd sentinel-backend && mkdir -p proto
          git clone --depth 1 https://github.com/MystenLabs/sui-apis.git proto/sui-apis

      - name: Check
        run: cd sentinel-backend && cargo check

      - name: Unit tests
        run: cd sentinel-backend && cargo test --lib

      - name: Mock tests (gRPC + GraphQL)
        run: cd sentinel-backend && cargo test --test grpc_mock_tests --test graphql_mock_tests

      - name: Live tests (gRPC against Sui testnet)
        run: cd sentinel-backend && cargo test --test grpc_integration_tests -- --ignored

      - name: Integration tests
        run: cd sentinel-backend && cargo test --test db_integration

      - name: Build release
        run: cd sentinel-backend && cargo build --release

      - name: Smoke test (backend starts and serves health check)
        run: |
          cd sentinel-backend && ./target/release/sentinel-backend &
          PID=$!
          for i in $(seq 1 30); do
            if curl -sf http://localhost:3001/api/health > /dev/null 2>&1; then
              echo "Health check passed"
              kill $PID
              exit 0
            fi
            sleep 1
          done
          echo "Backend failed to start within 30s"
          kill $PID 2>/dev/null
          exit 1

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Login to GHCR
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Docker metadata
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: ghcr.io/${{ github.repository }}/backend
          tags: |
            type=raw,value=${{ github.sha }}
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Build and push Docker image
        uses: docker/build-push-action@v7
        with:
          context: sentinel-backend
          provenance: false
          push: true
          load: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Smoke test (Docker container starts and serves health check)
        run: |
          docker run -d --name sentinel-smoke \
            --network host \
            -e DATABASE_URL=postgresql://sentinel:sentinel@localhost/sentinel \
            -e SENTINEL_API_PORT=3002 \
            ghcr.io/${{ github.repository }}/backend:${{ github.sha }}
          for i in $(seq 1 30); do
            if curl -sf http://localhost:3002/api/health > /dev/null 2>&1; then
              echo "Docker health check passed"
              docker stop sentinel-smoke
              exit 0
            fi
            sleep 1
          done
          echo "Docker container failed to start"
          docker logs sentinel-smoke
          docker stop sentinel-smoke
          exit 1

  frontend:
    name: Solid.js Frontend
    runs-on: ubuntu-24.04-arm
    steps:
      - uses: actions/checkout@v6
      - uses: oven-sh/setup-bun@v2

      - name: Install dependencies
        run: cd frontend && bun install

      - name: Type check + build
        run: cd frontend && bun run build

      - name: Test
        run: cd frontend && bun run test

  lint:
    name: Lint & Format
    runs-on: ubuntu-24.04-arm
    steps:
      - uses: actions/checkout@v6
      - uses: oven-sh/setup-bun@v2

      - name: Install deps
        run: cd frontend && bun install && cd ../ts-scripts && bun install

      - name: Biome (lint + format)
        run: bunx biome check .

      - name: Prettier (Move)
        run: cd ts-scripts && bun run fmt:move:check

  deploy-production:
    needs: [contracts, backend, frontend, lint]
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    uses: ./.github/workflows/deploy-production.yml
    secrets: inherit
    permissions:
      id-token: write
      contents: read


  deploy-dev:
    needs: [contracts, backend, frontend, lint]
    if: github.ref != 'refs/heads/main' && github.event_name == 'push'
    uses: ./.github/workflows/deploy-dev.yml
    secrets: inherit
    permissions:
      id-token: write
      contents: read
